<div class="col-sm-6 col-sm-offset-3">
  <% if @stylists.empty? %>
    <p class="text-center lead">Sorry, there are no stylists at this time.</p>
  <% else %>
    <p class="text-center lead">Here is a list of available stylists:</p>
    <ul>
    </ul>
  <% end %>
</div>
